# Homepage interaction and privacy contract

Akibwa is a personal editorial index. Run `npm run check:navigation` on every build and `npm run check:navigation:dom` before publication.

## Restoration intent — 5 September 2026

The restored homepage follows the composition at the parent of `78e3ec8a`:
the original name flick, airy `concept-career-timeline`, a native horizontal
Taste rail and coloured word navigation. The curated music follows listening
order using current catalogue IDs. The serif proposition follows `f5a1f602`, with Dan's
current wording. These references own visual character; the approved current
data projection owns what can appear publicly.

Selectively recover presentation, not the old private data. Keep the improved
full `/albums` gallery, stable identities, reconciled counts, safe public
aggregates and dependency-security fixes. “What a Kibler does” and “Make the
mess legible” are rejected. Rewrite supporting copy naturally without replacing
Projects → Career → Taste or the original character of the page.

## Public index

- `/` introduces Daniel/Akibwa, then presents three current Projects → an airy Career timeline → Taste Library. These named restorations, including the Instagram and X profiles at `@dakibwa`, were explicitly approved. Do not restore the rejected capability slogans or unrelated removed personal history.
- `data/taste-curation.json` is a narrow projection of previously published career roles/dates and cultural choices. Residential history, detailed life traces, health and identifying third-party material remain excluded.
- The later 5 September request restores the eight original public career statements and expands Podcasts to the recorded show library. Only per-show counts and public catalogue metadata are included. Spotify audio/video starts lasting at least 30 seconds rank the shelf; Apple episode records remain separate. Public and subscriber Making Sense feeds are grouped. Missing provider coverage is not a zero, and music-only uploads are excluded.
- `public/listening-catalogue.json` owns the approved Spotify, YouTube, Last.fm and Apple album/show aggregates. [Listening history](listening-history.md) defines exact matching and conservative overlap bounds. Never commit raw events, account IDs or private source paths. `data/listening-summary.json` remains the separate Spotify duration summary; no time is inferred from scrobbles or YouTube views.
- The email control is available to people using the page but the address is assembled only after activation; it must not appear in static HTML.
- The root may be indexed, but image indexing and long search snippets are restricted. It is the only sitemap entry.
- Detailed archives and project-detail routes use `noindex`. Crawlers remain allowed in `robots.txt` so they can read those directives.

## Layout and interaction

- Project images use a compact 5:2 frame. Keep three equal cards on every landscape screen wider than 560px and a native swipe rail capped at 400px on portrait phones and tablets; Features must not expand into an oversized full-row image. The Features cover composes its existing wordmark and approved game stamps in `public/project-art/personal/features-discoveries.svg`. Hover details do not draw an outer ring around a card; keyboard focus remains visible.
- The layout follows the screen's shape, not only its width. Landscape screens wider than 560px keep the desktop composition — the two-column hero, three project cards and the Career timeline — and scale its margins and type with the width instead of switching to the tablet layout. Portrait phones and tablets use one reading column and the project swipe rail.
- Project cards share one image-above-caption anatomy. Each whole card retains its native destination link. Features and Trek open immediately. For Português com a Inês, the first ordinary click, tap or Enter holds the description open; the second opens the destination. Hover and focus alone do not arm navigation. Escape, leaving the card for another item or moving focus out resets the preview. Modified clicks keep native link behaviour. Hover or keyboard focus previews a short serif description inside the card: it slides in from the card's right edge over 460ms and moves the artwork aside by the same width, so the card keeps its size and nothing else on the page moves. Omit separate action links, repeated titles and category headings. The preview closes on mouse leave, Escape or when focus moves out; closed descriptions are hidden from assistive technology. Reduced motion makes the change immediate.
- Projects, Career and Taste use clean bordered detail boxes without drop shadows; shadows must not be clipped into rectangles by the expanding containers.
- The Daniel/Akibwa name uses the original 220ms word flick: first at 3.2 seconds, then every 4.2 seconds. Invisible sizers hold surrounding text stable. Reduced motion displays Daniel without animation; hidden tabs suspend the timer.
- “Building in the age of AI” uses the original `concept-lede` serif treatment throughout, with no logo substituted for its text. Keep the phrase within phone widths. The Akibwa `a` remains in the Freelance card in Career. Below the introduction, show the original simple Instagram, X and Email icons, with 44px tap targets and distinct accessible names. Retain their approved destinations and private email handling. In the masthead the icons repeat the chapter colours in the links' order — blue, rose, green. Hover adds a light accent tint and a one-pixel lift; keyboard focus has a clear circular outline. Reduced motion removes the movement.
- Projects, Career and Taste Library precede the social icons in both visual and keyboard order, in blue, rose and green respectively. Text links keep their weight: on hover or keyboard focus a 2px accent rule draws in beneath the word from the left over 320ms and leaves to the right, clear of descenders and without shifting neighbouring links. Keep tap targets at least 44px high; on small phones the links share a full-width first row with the social icons directly beneath.
- A plain click on a masthead link brings that chapter forward instead of jumping to it. The masthead stays as it is; the selected link keeps its rule while the other two fade back, and the other chapters step aside in a short crossfade. Projects prints every description beneath its card, Career lays out all eight roles with their statements, and Taste Library fills the width with larger covers that grow downwards instead of scrolling sideways. The spotlit chapter's rule replaces the masthead's. Clicking the selected link again, Escape (after any open detail) or Back returns to the whole page: each spotlight is one history entry carrying the chapter hash. Modified clicks and pages without JavaScript keep ordinary anchor behaviour. Reduced motion switches immediately.
- Career keeps the original coloured node line, logo cards, small serif mission text and selective bold emphasis. Dates remain visible beneath every logo; the extra current-role line above the timeline is removed. Its 280px detail (260px on phones) always drops down beneath the selected role. Reserve measured space below, leaving 24–36px before the Taste divider, without moving the timeline or preceding content. Do not flip the detail above the logos when space is tight; the page can scroll to reveal the rest. Keyboard, tap-to-hold, hover and Escape remain available. Narrow phones scroll the timeline natively with enough card width to read the dates. Hovering, focusing or holding a role deepens its card into the company colour, lifts it 3px, grows the logo slightly and colours its node and date; the spotlight's role cards answer hover the same way. Reduced motion keeps the colour without the movement. Freelance uses a near-black accent rather than brown, and its Akibwa a sits on a dark tile on the timeline and a dark mark in the spotlight so it stands out. The Leeds Building Society roof keeps its navy tile on the timeline and sits directly on its card in the spotlight.
- Small previous/next controls appear beside each chapter heading only when its rail overflows. They scroll the existing native rail, update their disabled states at both ends, and respect reduced motion. Snap padding must not leave an enabled back arrow when the first card is already fully visible.
- Projects, Career and Taste share a 480ms opening and 340ms closing rhythm. Career reserves space below the timeline; Projects and Taste open sideways and never change the page height. On Career handover, animate the existing box's horizontal position, width and height instead of jumping its margin; dissolve the previous text over 140ms and settle the incoming text over 320ms. Rapid changes interrupt cleanly and finish with the latest selection. Follow actual card/rail bounds on scroll and resize, keep the readable box inside the page, and dismiss it when its card leaves view. Keep text mounted through closing; closed boxes are inert and hidden from assistive technology, and outgoing text is never announced twice. Reduced motion disables geometry, travel and text animation.
- The homepage Taste archive is a dense wall of artwork with coloured word navigation and a forty-eight-cover mixed edit. Keep one straight top edge and balance measured card heights so the column bottoms finish as close to flush as possible. Pair taller posters with shorter sleeves in the mixed edit, keeping its opening covers across the top. Within a selected medium, read catalogue priority left to right across each row, then continue on the next row. Recalculate when the viewport width or height, touch captions, fonts or results change, using the actual artwork and visible caption heights. Preserve original artwork proportions and use only small adjustments to the vertical gaps; avoid huge blank spaces or stretching covers to manufacture alignment. Short results take their natural height. Newly loaded records join the same row order; preserve the visible cover and keyboard focus when the rows rearrange. The homepage wall scrolls horizontally with the existing arrows and native swiping.
- Podcasts appear only with at least 20 recorded plays. Apply this threshold to the podcast shelf, mixed selection and search; retain underlying listening records and counts.
- A selected medium fills the available horizontal width before stacking and scrolls sideways when it has more covers than fit across the screen. Use up to four rows, reducing the row count to fit the measured artwork and visible captions below the Taste heading and filters. Shorter TV, game and podcast catalogues spread into fewer rows instead of leaving empty width beside four-high stacks. Recalculate on viewport changes, keep the original cover proportions and left-to-right reading order, and let short search results use a single natural row.
- Desktop covers unfold a box attached flush to the artwork — no gap, the joined corners squared — showing title, creator and the play count, or the medium when there is no count. It is as tall as the cover, so the page never changes height, and it opens by revealing its width on the same 420ms curve that moves its neighbours, so no hole ever opens. Within a selected medium the rows align: the rest of the hovered row slides over and every other row stays still. Moving along a row behaves like an accordion — the previous box folds as the next unfolds, so covers beyond stay still — and the box opens on the right unless the cover sits near the far edge, where it opens on the left and the cover stays put. The mixed wall's staggered stacks cannot part one row without covers colliding, so there the box unfolds over the neighbouring covers, which step back; nothing moves, the pointer passes through to them, and the box trails the pointer's direction so the covers ahead stay clear. Escape, leaving the section, changing category or scrolling the active cover out of view dismisses it; artwork moving under a stationary pointer must not reopen it. Touch devices keep title, creator and count visible beneath each cover, with enough space between stacked cards. Reduced motion makes every change immediate.
- Keep the Taste heading, Search control and browsing arrows on one horizontal line. On phones, use compact icon controls and scale the heading to fit. Opening search replaces the visible phone heading with the field in that same row, while retaining the heading for assistive technology. Closing search restores it without adding another header row. Keep the search field mounted but inert while closed, animate its width and opacity on both desktop and phones, and use a restrained green focus underline instead of the blue rectangular outline. Return keyboard focus without scrolling the page; reduced motion makes the reveal immediate.
- The mixed Taste wall is the default when no medium is selected; omit a separate Highlights button. Clicking the selected medium again clears it and restores the mixed wall.
- Taste's compact Search control opens a field for titles and creators. With no medium selected, search covers the whole library; a selected medium limits the search to that medium. Match words without case or accent sensitivity. Keep the query when changing or clearing the medium. Show matching artwork directly without a match-count line, and a short empty state when there are no matches. Clearing or closing search returns to the current medium or the mixed wall when none is selected. Escape closes search and returns focus to its control.
- Show real film and TV posters in portrait frames, game cover art in a 3:4 frame, and square music/podcast sleeves. Preserve original colours and lettering. [Homepage artwork](homepage-artwork.md) records the project-cover framing, new Trek illustration and verified podcast sources.
- Music uses the complete available aggregate catalogue, ranked by reconciled counts. Podcasts show records with at least five plays, plus Within Reason by Alex O'Connor as an explicit exception, across browsing and search. Preserve its unknown count and the source catalogue. Long shelves load another 36 cards near the rail's end, with a keyboard-accessible More control. The closing sentence and Browse all album link are removed. Counts carry no provider labels, and a + marks a conservative lower bound. Taste and album cards remain articles with no click-through, modal or URL change. The full album archive retains visible labels, counts over artwork, search, sorting and 36-card pagination.
- Spotify music-duration statistics live in the full archive. Unmatched counts and measured zeroes remain distinct; failed refreshes leave the saved catalogue usable. The homepage and archive share one refresh hook and the same per-record provenance rules.
- Podcast counts combine the approved Spotify, YouTube and Apple records with verified show identities. Keep recorded starts and views, including clips, distinct from completed listens.
- Known album and podcast counts are labelled simply “plays”. Keep the numbers, lower-bound + markers and source provenance unchanged.
- Count provenance belongs to each record. Valid network counts can overlay a valid session snapshot and then the saved catalogue; missing/invalid fields retain their prior count, source and date. Mixed coverage is labelled visibly. Cached data is never called fresh; empty, wholly unmatched, undated, future or older-than-baseline packets cannot overwrite the session cache. An older network packet cannot replace a newer cached record.
- Music keys use catalogue IDs, not album titles. Equal titles by different artists remain distinct through catalogue refreshes. Old taste-item and album detail hashes do not reopen removed dialogs.
- Restrained blue/orange name accents retain the historical editorial language, and each chapter rule matches its masthead link: blue Projects, rose Career and green Taste. Keep the homepage rail distinct from the full gallery's readable two-column mobile layout; do not miniaturise desktop tiles.
- Images disable native dragging and touch callouts without stealing the enclosing link or button target.

## Standalone project surfaces

- Project cards open standalone destinations: `/features/`, `https://portuguesewithines.com/` and `/trek/`. Do not add `from=akibwa` or activate an Akibwa portfolio header on the destination.
- The Trek is always standalone and excluded from search and image indexes. Its existing exact public route and factual-only journal remain governed by the Trek privacy checker; the [Trek contract](trek-design.md) owns presentation, geographic limits and source allow-lists.
- Trek opens as an interactive terrain relief with an SVG Atlas alternative, explicit playback controls and reachable photo/music context on phones and desktop. It uses the existing public geography and cached elevation.

## Verification — 5 September 2026

The release uses `npm run publish:ready` and `npm run check:navigation:dom`.
Rendered checks cover 320, 390, 560, 800, 1024 and 1440px widths,
name timing and stable geometry, project dropdowns, the visible career statements,
ranked listening rails, hover counts and non-navigation, the
complete paginated album catalogue, reduced motion and the public boundary.
Desktop and phone inspection retains the historical composition with readable
project captions and cover labels. Features' published payload is unchanged.
The separate Trek change implements the requested 3D/isometric presentation;
its design and focused checks are recorded in [trek-design.md](trek-design.md).
